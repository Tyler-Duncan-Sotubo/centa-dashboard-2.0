import HolidaySettings from "@/features/leave/settings/holidays/HolidaySettings";

const page = () => {
  return (
    <div className="p-5">
      <HolidaySettings />
    </div>
  );
};

export default page;
