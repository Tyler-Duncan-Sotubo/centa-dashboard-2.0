export enum EmploymentType {
  FullTime = "full_time",
  PartTime = "part_time",
  Contract = "contract",
  Internship = "internship",
  Temporary = "temporary",
  Freelance = "freelance",
}

export enum JobType {
  OnSite = "onsite",
  Remote = "remote",
  Hybrid = "hybrid",
}
